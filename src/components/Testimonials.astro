---
// Testimonials.astro — Críticas & Reseñas Gastronómicas Multi-Color
const testimonials = [
  {
    id: 1,
    quote: "La Wagyu Melanosporum no es simplemente la mejor hamburguesa de Buenos Aires: es una lección magistral de equilibrio graso, acidez y textura. El pan de papa tiene una esponjosidad sideral.",
    name: "Guillermo Von Stecher",
    role: "Crítico Gastronómico · Guía Gourmet BA",
    avatar: "https://images.pexels.com/photos/415829/pexels-photo-415829.jpeg?auto=compress&cs=tinysrgb&w=120&h=120&dpr=1",
    rating: 5,
    tagColor: "#FF5722",
  },
  {
    id: 2,
    quote: "Vivir el Chef’s Table frente a las brasas de quebracho y probar el smash de ojo de bife con maridaje de Pinot Noir fue una experiencia que redefine por completo la cultura burger internacional.",
    name: "Federico Marchetti",
    role: "Sommelier Internacional & Autor",
    avatar: "https://images.pexels.com/photos/220453/pexels-photo-220453.jpeg?auto=compress&cs=tinysrgb&w=120&h=120&dpr=1",
    rating: 5,
    tagColor: "#FFB703",
  },
  {
    id: 3,
    quote: "El rigor que ponen en la fermentación del brioche y la maduración en seco de los cortes sitúa a Élite al nivel de los ateliers gastronómicos más exigentes de París o Tokio.",
    name: "Claire Delacroix",
    role: "Food & Travel Columnist · Le Monde Gourmet",
    avatar: "https://images.pexels.com/photos/774909/pexels-photo-774909.jpeg?auto=compress&cs=tinysrgb&w=120&h=120&dpr=1",
    rating: 5,
    tagColor: "#06D6A0",
  },
  {
    quote: 'El nivel biomecánico y la precisión en cada plataforma de levantamiento Eleiko no tienen comparación en el país. Mejoré mi total en Snatch y Clean & Jerk en 22 kg en solo 6 meses bajo la supervisión de los head coaches.',
    name: 'Valentina Rossi',
    role: 'Atleta Nacional de Halterofilia & Miembro APEX',
    tagColor: '#CCFF00',
    avatar: 'https://images.pexels.com/photos/3757954/pexels-photo-3757954.jpeg?auto=compress&cs=tinysrgb&w=200&h=200&dpr=1',
    rating: 5,
  },
  {
    quote: 'Como fundador con jornadas de 14 horas, el sistema de periodización basado en HRV y las sesiones de crioterapia a -110°C transformaron mi energía física y claridad mental. Es un santuario de alto rendimiento.',
    name: 'Lucas Benítez',
    role: 'Tech Founder & Atleta Hyrox Pro',
    tagColor: '#00F0FF',
    avatar: 'https://images.pexels.com/photos/1552249/pexels-photo-1552249.jpeg?auto=compress&cs=tinysrgb&w=200&h=200&dpr=1',
    rating: 5,
  },
  {
    quote: 'El rigor fisiológico y la selección de maquinaria Prime y Arsenal Strength reducen a cero el impacto articular en sentadillas pesadas. Para atletas que buscan longevidad y fuerza real, APEX es el estándar definitivo.',
    name: 'Dra. Camila Duarte',
    role: 'Médica Especialista en Medicina Deportiva',
    tagColor: '#FF5500',
    avatar: 'https://images.pexels.com/photos/3823063/pexels-photo-3823063.jpeg?auto=compress&cs=tinysrgb&w=200&h=200&dpr=1',
    rating: 5,
  },
  {
    quote: 'El tatami olímpico y el ring de boxeo profesional con sacos Aquabag ofrecen un entorno de entrenamiento de combate de primer nivel mundial. La comunidad empuja tus límites en cada round.',
    name: 'Mateo Silva',
    role: 'Luchador BJJ Black Belt & Boxeo Competitivo',
    tagColor: '#FFFFFF',
    avatar: 'https://images.pexels.com/photos/4754146/pexels-photo-4754146.jpeg?auto=compress&cs=tinysrgb&w=200&h=200&dpr=1',
    rating: 5,
  },
];
---

<section class="section testimonials" id="testimonios" aria-labelledby="testimonials-heading">
  <div class="container">
    <div class="testimonials__header reveal">
      <span class="eyebrow">VOCES DE ALTO RENDIMIENTO</span>
      <span class="volt-line volt-line--center"></span>
      <h2 class="display-heading testimonials__heading" id="testimonials-heading">
        Historias de <span class="highlight-volt">Transformación</span>
      </h2>
    </div>

    <!-- Carousel -->
    <div class="carousel reveal" role="region" aria-label="Testimonios de atletas" aria-roledescription="carrusel">
      <div class="carousel__track" id="carousel-track" aria-live="polite">
        {testimonials.map((t, i) => (
          <article
            class="testimonial-card"
            role="group"
            aria-roledescription="diapositiva"
            aria-label={`Testimonio ${i + 1} de ${testimonials.length}`}
            aria-hidden={i !== 0 ? 'true' : 'false'}
          >
            <!-- Stars -->
            <div class="testimonial-stars" aria-label={`${t.rating} de 5 estrellas`}>
              {Array.from({ length: t.rating }).map(() => (
                <svg width="18" height="18" viewBox="0 0 24 24" fill="#CCFF00" aria-hidden="true">
                  <polygon points="12,2 15.09,8.26 22,9.27 17,14.14 18.18,21.02 12,17.77 5.82,21.02 7,14.14 2,9.27 8.91,8.26"/>
                </svg>
              ))}
            </div>
            <blockquote class="testimonial-quote">
              <p>"{t.quote}"</p>
            </blockquote>
            <footer class="testimonial-author">
              <img
                src={t.avatar}
                alt={`Foto de ${t.name}`}
                class="testimonial-avatar"
                loading="lazy"
                width="56"
                height="56"
              />
              <div>
                <cite class="testimonial-name">{t.name}</cite>
                <span class="testimonial-role" style={`color: ${t.tagColor};`}>{t.role}</span>
              </div>
            </footer>
          </article>
        ))}
      </div>

      <!-- Controls -->
      <div class="carousel__controls">
        <button class="carousel__btn" id="carousel-prev" aria-label="Testimonio anterior">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true">
            <path d="M19 12H5M12 19l-7-7 7-7"/>
          </svg>
        </button>

        <div class="carousel__dots" role="tablist" aria-label="Seleccionar testimonio" id="carousel-dots">
          {testimonials.map((_, i) => (
            <button
              class={`carousel__dot${i === 0 ? ' active' : ''}`}
              role="tab"
              aria-selected={i === 0 ? 'true' : 'false'}
              aria-label={`Ir al testimonio ${i + 1}`}
              data-index={i}
            ></button>
          ))}
        </div>

        <button class="carousel__btn" id="carousel-next" aria-label="Siguiente testimonio">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true">
            <path d="M5 12h14M12 5l7 7-7 7"/>
          </svg>
        </button>
      </div>
    </div>
  </div>
</section>

<style>
.testimonials {
  background: var(--color-bg);
  text-align: center;
  border-top: 1px solid rgba(255, 255, 255, 0.06);
  border-bottom: 1px solid rgba(255, 255, 255, 0.06);
}

.testimonials__header {
  margin-bottom: 3.5rem;
}

.testimonials__heading {
  font-size: clamp(2.4rem, 4vw, 3.8rem);
  color: #FFFFFF;
  margin-top: 0.5rem;
}

.highlight-volt {
  background: var(--grad-volt);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

/* Carousel */
.carousel {
  max-width: 880px;
  margin-inline: auto;
  position: relative;
}

.carousel__track {
  position: relative;
  min-height: 280px;
  display: grid;
  grid-template-columns: 1fr;
}

/* Card */
.testimonial-card {
  grid-area: 1 / 1;
  background: var(--color-bg-card);
  padding: clamp(2.5rem, 5vw, 4rem);
  border: 1px solid rgba(255, 255, 255, 0.1);
  border-radius: var(--radius-md);
  backdrop-filter: blur(16px);
  opacity: 0;
  transform: translateX(20px);
  transition: opacity 0.5s ease, transform 0.5s ease;
  pointer-events: none;
  position: relative;
  box-shadow: var(--shadow-lg);
}

.testimonial-card.active {
  opacity: 1;
  transform: translateX(0);
  pointer-events: auto;
}

.testimonial-stars {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 6px;
  margin-bottom: 1.5rem;
}

.testimonial-quote {
  font-family: var(--font-body);
  font-size: clamp(1.1rem, 2vw, 1.35rem);
  font-weight: 400;
  color: #FFFFFF;
  line-height: 1.7;
  margin-bottom: 2rem;
}

.testimonial-author {
  display: flex;
  align-items: center;
  gap: 1rem;
  justify-content: center;
}

.testimonial-avatar {
  width: 56px;
  height: 56px;
  border-radius: 50%;
  object-fit: cover;
  border: 2px solid var(--color-volt);
  flex-shrink: 0;
  box-shadow: 0 0 16px rgba(204, 255, 0, 0.4);
}

.testimonial-name {
  display: block;
  font-style: normal;
  font-family: var(--font-heading);
  font-size: 1.15rem;
  font-weight: 800;
  color: #FFFFFF;
  margin-bottom: 0.2rem;
  text-transform: uppercase;
}

.testimonial-role {
  font-size: 0.75rem;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  font-weight: 700;
}

/* Controls */
.carousel__controls {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 1.5rem;
  margin-top: 2rem;
}

.carousel__btn {
  width: 46px;
  height: 46px;
  border: 1px solid rgba(255, 255, 255, 0.15);
  background: var(--color-bg-card);
  color: var(--color-volt);
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  transition: all var(--transition-base);
}

.carousel__btn:hover {
  border-color: var(--color-volt);
  background: var(--color-volt);
  color: #080C14;
  transform: scale(1.1);
  box-shadow: 0 0 20px rgba(204, 255, 0, 0.4);
}

.carousel__dots {
  display: flex;
  gap: 0.6rem;
  align-items: center;
}

.carousel__dot {
  width: 10px;
  height: 10px;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.2);
  border: none;
  padding: 0;
  transition: all var(--transition-base);
  cursor: pointer;
}

.carousel__dot.active {
  background: var(--color-volt);
  transform: scale(1.3);
  box-shadow: 0 0 10px rgba(204, 255, 0, 0.8);
}
</style>

<script>
  const track = document.getElementById('carousel-track');
  const cards = track?.querySelectorAll<HTMLElement>('.testimonial-card');
  const dots = document.querySelectorAll<HTMLButtonElement>('.carousel__dot');
  const prevBtn = document.getElementById('carousel-prev');
  const nextBtn = document.getElementById('carousel-next');

  if (cards && dots && cards.length > 0) {
    let current = 0;
    let autoPlayTimer: ReturnType<typeof setInterval>;

    function goTo(index: number) {
      if (!cards) return;
      cards[current].classList.remove('active');
      cards[current].setAttribute('aria-hidden', 'true');
      if (dots[current]) {
        dots[current].classList.remove('active');
        dots[current].setAttribute('aria-selected', 'false');
      }

      current = (index + cards.length) % cards.length;

      cards[current].classList.add('active');
      cards[current].setAttribute('aria-hidden', 'false');
      if (dots[current]) {
        dots[current].classList.add('active');
        dots[current].setAttribute('aria-selected', 'true');
      }
    }

    function startAutoPlay() {
      autoPlayTimer = setInterval(() => goTo(current + 1), 6000);
    }

    function resetAutoPlay() {
      clearInterval(autoPlayTimer);
      startAutoPlay();
    }

    goTo(0);
    startAutoPlay();

    prevBtn?.addEventListener('click', () => { goTo(current - 1); resetAutoPlay(); });
    nextBtn?.addEventListener('click', () => { goTo(current + 1); resetAutoPlay(); });
    dots.forEach((dot, i) => {
      dot.addEventListener('click', () => { goTo(i); resetAutoPlay(); });
    });

    track?.addEventListener('mouseenter', () => clearInterval(autoPlayTimer));
    track?.addEventListener('mouseleave', startAutoPlay);
  }
</script>
